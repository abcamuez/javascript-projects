// create an array variable containing the name
let languages = ["JS", "Java", "C#", "Python"];

for (let i = 0; i < languages.length; i++) {
   console.log(languages[i]);
}
// write a for loop that prints each name on a different line
let name = ['Aislyn Bedoya','Karena Tangarife','Domino Bedoya', 'Thai tangarife'];
for (let i = 0; i < name.length; i++) {
    console.log(name[i])
}